POSTGRES_PASSWORD=CHANGEME
POSTGRES_USER=CHANGEME
POSTGRES_DB=CHANGEME
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
# Native Ollama (recommended for Mac — uses GPU):
OLLAMA_HOST=http://host.docker.internal:11434
# Docker Ollama (CPU only, use with: docker compose --profile docker-ollama up -d):
# OLLAMA_HOST=http://ollama:11434
JWT_SECRET=CHANGEME
ENCRYPTION_KEY=CHANGEME
DATA_PATH=CHANGEME
# Optional for EC2/public frontend access (e.g. http://<EC2_IP>:3000 or https://app.example.com)
FRONTEND_ORIGIN=
